import getData from "./app.js";

let result = await getData(5) // burada yazdığımız 5 değeri userId  ve id olacak kısıma verdiğimiz deger.2 -3 yazsak userId: 2  id:2 olur örnek.
console.log(result);